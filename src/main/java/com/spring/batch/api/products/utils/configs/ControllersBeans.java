package com.spring.batch.api.products.utils.configs;

import org.springframework.context.annotation.Configuration;

@Configuration
public class ControllersBeans {

    /*@Bean
    public ProductController<BookDto> bookController(BookPresenter presenter, BookProductGateway gateway, BookBusiness business) {
        return new ProductController<>(presenter, gateway, business, BookDto.class);
    }

    @Bean
    public ProductController<ElectronicDto> electronicController(ElectronicPresenter presenter, ElectronicProductGateway gateway, ElectronicBusiness business) {
        return new ProductController<>(presenter, gateway, business, ElectronicDto.class);
    }*/
}
